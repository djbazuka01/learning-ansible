when issue with ssh url just http url 

git clone http

git add inventory
git status
git commit -m "first verison of inventory file"i



when issue with commiting just use the personnel token here
git commit origin main (use the token instead of the password)
